# NNews Docker Environment Configuration
# Copy this file to .env and update with your values

# ==========================================
# PostgreSQL Database Configuration
# ==========================================
POSTGRES_DB=nnews_db
POSTGRES_USER=nnews_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_PORT=5433

# ==========================================
# Connection String (for local development)
# ==========================================
CONNECTION_STRING=Host=localhost;Port=5433;Database=nnews_db;Username=nnews_user;Password=your_secure_password_here

# ==========================================
# JWT Configuration
# ==========================================
# Generate a secure random string for production
# You can use: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_key_min_32_chars_recommended_64_chars

# ==========================================
# NNews API Configuration
# ==========================================
API_HTTP_PORT=5007
API_HTTPS_PORT=5008

# ==========================================
# NNews App Configuration
# ==========================================
# Frontend application port
NNEWS_APP_PORT=5009

# Vite API URLs (used during build)
# These are embedded into the frontend during the Docker build process
VITE_API_URL=http://localhost:5007
VITE_NNEWS_API_URL=https://localhost:5008

# Node environment
# Options: development, production
NODE_ENV=production

# ==========================================
# ASP.NET Core Environment
# ==========================================
# Options: Development, Staging, Production, Docker
# - Development: Uses appsettings.Development.json, enables Swagger, detailed errors
# - Production: Uses appsettings.Production.json, minimal logging, optimized for production
# - Docker: Uses appsettings.Docker.json, container-specific settings
ASPNETCORE_ENVIRONMENT=Production

# ==========================================
# SSL Certificate (for HTTPS support)
# ==========================================
CERTIFICATE_PATH=/app/certs/certificate.pfx
CERTIFICATE_PASSWORD=your_certificate_password_here
CERTIFICATE_DIRECTORY=./certs

# ==========================================
# Additional Configuration (Optional)
# ==========================================
# ASPNETCORE_ENVIRONMENT=Production
# LOG_LEVEL=Information
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ==========================================
# Notes
# ==========================================
# - Change all passwords before deploying to production
# - Keep this file (.env) in .gitignore
# - Use strong, randomly generated passwords
# - JWT_SECRET should be at least 32 characters (64+ recommended)
# - For production, consider using environment variables or secrets management
# - NNEWS_API_HTTP_PORT: External HTTP port for NNews API (default: 5007)
# - NNEWS_API_HTTPS_PORT: External HTTPS port for NNews API (default: 5008)
# - CERTIFICATE_PATH: Path to SSL certificate inside container
# - CERTIFICATE_DIRECTORY: Local directory containing SSL certificates